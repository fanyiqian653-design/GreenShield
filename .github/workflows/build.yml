name: Build iOS App

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Create IPA
      run: |
        mkdir -p app/Payload/MyApp.app
        echo '#!/bin/sh' > app/Payload/MyApp.app/MyApp
        chmod +x app/Payload/MyApp.app/MyApp
        echo '<?xml version="1.0" encoding="UTF-8"?>' > app/Payload/MyApp.app/Info.plist
        echo '<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">' >> app/Payload/MyApp.app/Info.plist
        echo '<plist version="1.0">' >> app/Payload/MyApp.app/Info.plist
        echo '<dict>' >> app/Payload/MyApp.app/Info.plist
        echo '    <key>CFBundleIdentifier</key>' >> app/Payload/MyApp.app/Info.plist
        echo '    <string>com.greenshield.modifier</string>' >> app/Payload/MyApp.app/Info.plist
        echo '    <key>CFBundleName</key>' >> app/Payload/MyApp.app/Info.plist
        echo '    <string>GreenShield</string>' >> app/Payload/MyApp.app/Info.plist
        echo '    <key>CFBundleDisplayName</key>' >> app/Payload/MyApp.app/Info.plist
        echo '    <string>绿盾版本修改器</string>' >> app/Payload/MyApp.app/Info.plist
        echo '    <key>CFBundleVersion</key>' >> app/Payload/MyApp.app/Info.plist
        echo '    <string>1.0</string>' >> app/Payload/MyApp.app/Info.plist
        echo '    <key>CFBundleShortVersionString</key>' >> app/Payload/MyApp.app/Info.plist
        echo '    <string>1.0</string>' >> app/Payload/MyApp.app/Info.plist
        echo '    <key>LSRequiresIPhoneOS</key>' >> app/Payload/MyApp.app/Info.plist
        echo '    <true/>' >> app/Payload/MyApp.app/Info.plist
        echo '    <key>CFBundleExecutable</key>' >> app/Payload/MyApp.app/Info.plist
        echo '    <string>MyApp</string>' >> app/Payload/MyApp.app/Info.plist
        echo '</dict>' >> app/Payload/MyApp.app/Info.plist
        echo '</plist>' >> app/Payload/MyApp.app/Info.plist
        cd app && zip -r GreenShield.ipa Payload/
    
    - name: Upload IPA
      uses: actions/upload-artifact@v4
      with:
        name: GreenShield
        path: app/GreenShield.ipa
        retention-days: 30
